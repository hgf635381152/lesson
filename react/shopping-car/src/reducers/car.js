const initialState = {
  addedIds: [],
  quantityById: {}
}

const car = (state = initialState, action) => {
  switch(action.type){
    default: 
      return state
  }
}

export default car